# ServerTools

This repository hosts a bunch of shell scripts that makes a server admin's life easier.
Scripts are grouped together in directories with roughly the same subset of functions.

All scripts are selfcontained and have no dependencies.

All scripts are kept according to the KISS principle.

